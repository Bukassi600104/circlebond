rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Milestone 1 starts from a secure baseline. Later milestones will add
    // narrowly scoped rules for avatars, circle images, and payment proofs.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
