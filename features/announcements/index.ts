export type Announcement = {
  id: string;
  title: string;
  body: string;
  pinned: boolean;
  commentsEnabled: boolean;
  authorId: string;
  authorName: string;
  createdAt: string;
  updatedAt: string;
};
